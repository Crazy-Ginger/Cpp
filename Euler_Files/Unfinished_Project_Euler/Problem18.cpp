#include <iostream>
#include <vector>
#include <fstream>
#include <string>
using namespace std;

int main ()
	{
	int row = 0, column = 0;
	vector <vector <int>> triangle(row);
	ifstream 	reader;
	if (!reader)
		{
		cout << "Error opening trianlges" << endl;
		return -1;
		}
	int linesize = 1;
	string temp;
	while (!reader.eof())
		{
		
		triangle
		linesize ++;
		}
	
	
	char release;
	cin >> release;
	return 0;
	}